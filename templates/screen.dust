<div class="prefsScreen">
	<h1 class="prefsHeader>PreferencesScreen</h1>
	<form name="prefs">
	{#categories}
		{>category id="{@idx}" /}
		{@sep}<hr class="prefsHr"/>{/sep}
	{/categories}
	<button class="prefsButton prefsSubmit" id="submit" type="submit" text="Go" />
	</form>
</div>

